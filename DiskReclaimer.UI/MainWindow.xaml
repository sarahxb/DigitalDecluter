<Window x:Class="DiskReclaimer.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DiskReclaimer.UI"
        xmlns:converters="clr-namespace:DiskReclaimer.UI.Converters"
        mc:Ignorable="d"
        Title="Disk Reclaimer" Height="600" Width="1000">
    <Window.Resources>
        <converters:StringListToTextConverter x:Key="StringListToTextConverter" />
        <converters:CategoryBreakdownToTextConverter x:Key="CategoryBreakdownToTextConverter" />
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <TextBox Text="{Binding RootPath, UpdateSourceTrigger=PropertyChanged}" Width="500" VerticalContentAlignment="Center" />
            <Button Content="Browse..." Click="OnBrowseClicked" Margin="8,0,0,0" Padding="8,2" />
            <Button Content="Scan" Command="{Binding ScanCommand}" Margin="8,0,0,0" Padding="8,2" />
            <Button Content="Cancel" Command="{Binding CancelScanCommand}" IsEnabled="{Binding IsScanning}" Margin="8,0,0,0" Padding="8,2" />
            <Button Content="Export CSV..." Click="OnExportCsvClicked" Margin="24,0,0,0" Padding="8,2" />
        </StackPanel>

        <TextBlock Text="{Binding StatusMessage}" Grid.Row="1" Margin="0,0,0,8" />

        <TabControl Grid.Row="2">
            <TabItem Header="Recommendations">
                <DataGrid ItemsSource="{Binding Recommendations}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="" Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Reveal" Padding="6,0"
                                            Command="{Binding DataContext.RevealInExplorerCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding TargetPath}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Path" Binding="{Binding TargetPath}" Width="3*" />
                        <DataGridTextColumn Header="Confidence" Binding="{Binding ConfidenceTier}" Width="*" />
                        <DataGridTextColumn Header="Reclaimable (bytes)" Binding="{Binding ReclaimableBytes}" Width="*" />
                        <DataGridTextColumn Header="Reasons" Binding="{Binding Reasons, Converter={StaticResource StringListToTextConverter}}" Width="2*" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Files">
                <DataGrid ItemsSource="{Binding Files}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="" Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Reveal" Padding="6,0"
                                            Command="{Binding DataContext.RevealInExplorerCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding Record.FullPath}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Path" Binding="{Binding Record.FullPath}" Width="3*" />
                        <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*" />
                        <DataGridTextColumn Header="Size (bytes)" Binding="{Binding Record.SizeBytes}" Width="*" />
                        <DataGridTextColumn Header="Last Modified" Binding="{Binding Record.LastModifiedUtc}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Detected Folders">
                <DataGrid ItemsSource="{Binding Folders}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="" Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Reveal" Padding="6,0"
                                            Command="{Binding DataContext.RevealInExplorerCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding Path}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="2*" />
                        <DataGridTextColumn Header="Type" Binding="{Binding FolderType}" Width="*" />
                        <DataGridTextColumn Header="Size (bytes)" Binding="{Binding AggregateSizeBytes}" Width="*" />
                        <DataGridTextColumn Header="File Count" Binding="{Binding FileCount}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Insights">
                <DataGrid ItemsSource="{Binding Insights}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="" Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Reveal" Padding="6,0"
                                            Command="{Binding DataContext.RevealInExplorerCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding FolderPath}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Folder" Binding="{Binding FolderPath}" Width="2*" />
                        <DataGridTextColumn Header="Total Size (bytes)" Binding="{Binding TotalSizeBytes}" Width="*" />
                        <DataGridTextColumn Header="File Count" Binding="{Binding FileCount}" Width="*" />
                        <DataGridTextColumn Header="Category Breakdown (bytes)"
                                             Binding="{Binding CategoryBreakdown, Converter={StaticResource CategoryBreakdownToTextConverter}}" Width="3*" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="History">
                <DataGrid ItemsSource="{Binding History}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Root Path" Binding="{Binding RootPath}" Width="3*" />
                        <DataGridTextColumn Header="Completed" Binding="{Binding ScanCompletedUtc}" Width="2*" />
                        <DataGridTextColumn Header="Files" Binding="{Binding FilesScanned}" Width="*" />
                        <DataGridTextColumn Header="Folders" Binding="{Binding FoldersDetected}" Width="*" />
                        <DataGridTextColumn Header="Recommendations" Binding="{Binding RecommendationCount}" Width="*" />
                        <DataGridTextColumn Header="Reclaimable (bytes)" Binding="{Binding TotalReclaimableBytes}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
